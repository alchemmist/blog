alchemmist.xyz {
    root * /srv/site
    encode gzip

    handle_path /cv* {
        file_server {
            root * /srv/site
            index cv.html
        }
    }

    handle {
        try_files {path} /index.html
        file_server
    }
}

